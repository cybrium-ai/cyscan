id:        CBR-PYTH-MANUAL_DEFAULTDICT_LIST_CREATE
title:     "Manual Defaultdict List Create"
severity:  high
languages: ['python']
source:    semgrep/manual-defaultdict-list-create
message: |
  manually creating a defaultdict - use collections.defaultdict(list)
pattern: |
  $DICT = {}
  ...
  for $KEY, $VALUE in $OTHERDICT.items():
      ...
      if $KEY not in $DICT:
          ...
          $DICT[$KEY] = []
          ...
      $DICT[$KEY].append(...)
  
