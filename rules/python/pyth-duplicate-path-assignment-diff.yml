id:        CBR-PYTH-DUPLICATE_PATH_ASSIGNMENT_DIFF
title:     "Duplicate Path Assignment Different Names"
severity:  high
languages: ['python']
source:    semgrep/duplicate-path-assignment-different-names
message: |
  path for `$URL` is assigned twice with different names
pattern: |
  [..., django.urls.path('$URL', $VIEW, name='$NAME', ...), ..., django.urls.path('$URL', $VIEW, name='$OTHER_NAME', ...), ...]
  
