id:        CBR-PYTH-JWT_PYTHON_EXPOSED_DATA
title:     "Jwt Python Exposed Data"
severity:  high
languages: ['python']
cwe:       ['CWE-522']
source:    semgrep/jwt-python-exposed-data
message: |
  The object is passed strictly to jwt.encode(...) Make sure that sensitive information is not exposed through JWT token payload.
pattern: |
  jwt.encode($INPUT,...)
