id:        CBR-PYTH-MANUAL_COUNTER_CREATE
title:     "Manual Counter Create"
severity:  high
languages: ['python']
source:    semgrep/manual-counter-create
message: |
  manually creating a counter - use collections.Counter
pattern: |
  $DICT = {}
  ...
  for $KEY, $VALUE in $OTHERDICT.items():
      ...
      if $KEY not in $DICT:
          ...
          $DICT[$KEY] = 0
          ...
      $DICT[$KEY] += 1
  
