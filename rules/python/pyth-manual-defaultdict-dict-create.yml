id:        CBR-PYTH-MANUAL_DEFAULTDICT_DICT_CREATE
title:     "Manual Defaultdict Dict Create"
severity:  high
languages: ['python']
source:    semgrep/manual-defaultdict-dict-create
message: |
  manually creating a defaultdict - use collections.defaultdict(dict)
pattern: |
  $DICT = {}
  ...
  for $KEY, $VALUE in $OTHERDICT.items():
      ...
      if $KEY not in $DICT:
          ...
          $DICT[$KEY] = {}
          ...
      $DICT[$KEY].update(...)
  
