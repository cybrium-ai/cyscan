id:        CBR-TERR-AWS-0067
title:     "Ensure that lambda function permission has a source arn specified"
severity:  critical
languages: ['terraform']
cwe:       ['CWE-693']
source:    trivy/aws-lambda-restrict-source-arn
message: |
  When the principal is an AWS service, the ARN of the specific resource within that service to grant permission to.
  Without this, any resource from principal will be granted permission – even if that resource is from another account.
  For S3, this should be the ARN of the S3 Bucket. For CloudWatch Events, this should be the ARN of the CloudWatch Events Rule. For API Gateway, this should be the ARN of the API
  
  Remediation: Always provide a source arn for Lambda permissions
pattern: |
  resource\s+"aws_lambda_function"
