id:        CBR-TERR-AWS-0012
title:     "CloudFront distribution allows unencrypted (HTTP) communications."
severity:  critical
languages: ['terraform']
cwe:       ['CWE-693']
source:    trivy/aws-cloudfront-enforce-https
message: |
  Plain HTTP is unencrypted and human-readable. This means that if a malicious actor was to eavesdrop on your connection, they would be able to see all of your data flowing back and forth.
  You should use HTTPS, which is HTTP over an encrypted (TLS) connection, meaning eavesdroppers cannot read your traffic.
  
  Remediation: Only allow HTTPS for CloudFront distribution communication
regex: |
  resource\s+"aws_cloudfront_distribution"
