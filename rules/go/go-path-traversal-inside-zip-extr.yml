id:        CBR-GO-PATH_TRAVERSAL_INSIDE_ZIP_EXTR
title:     "Path Traversal Inside Zip Extraction"
severity:  high
languages: ['go']
cwe:       ['CWE-22']
source:    semgrep/path-traversal-inside-zip-extraction
message: |
  File traversal when extracting zip archive
pattern: |
  reader, $ERR := zip.OpenReader($ARCHIVE)
  ...
  for _, $FILE := range reader.File {
    ...
    path := filepath.Join($TARGET, $FILE.Name)
    ...
  }
  
