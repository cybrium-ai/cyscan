id:        CBR-SEC-GL-GITLAB_RUNNER_AUTHENTICATION_TOKEN
title:     "Discovered a GitLab Runner Authentication Token, posing a risk to CI/CD pipeline integrity and unaut"
severity:  high
languages: ['generic']
cwe:       ['CWE-798']
source:    gitleaks/gitlab-runner-authentication-token
message: |
  Discovered a GitLab Runner Authentication Token, posing a risk to CI/CD pipeline integrity and unauthorized access.
  
  Remediation: Remove the secret, rotate it immediately, and
  store it in a secret manager or environment variable.
regex: |
  glrt-[0-9a-zA-Z_\-]{20}
