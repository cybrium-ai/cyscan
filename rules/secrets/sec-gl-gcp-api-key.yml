id:        CBR-SEC-GL-GCP_API_KEY
title:     "Uncovered a GCP API key, which could lead to unauthorized access to Google Cloud services and data b"
severity:  high
languages: ['generic']
cwe:       ['CWE-798']
source:    gitleaks/gcp-api-key
message: |
  Uncovered a GCP API key, which could lead to unauthorized access to Google Cloud services and data breaches.
  
  Remediation: Remove the secret, rotate it immediately, and
  store it in a secret manager or environment variable.
regex: |
  \b(AIza[\w-]{35})(?:[\x60'"\s;]|\\[nr]|$)
