id:        CBR-SEC-GL-AIRTABLE_PERSONNAL_ACCESS_TOKEN
title:     "Uncovered a possible Airtable Personal AccessToken, potentially compromising database access and lea"
severity:  high
languages: ['generic']
cwe:       ['CWE-798']
source:    gitleaks/airtable-personnal-access-token
message: |
  Uncovered a possible Airtable Personal AccessToken, potentially compromising database access and leading to data leakage or alteration.
  
  Remediation: Remove the secret, rotate it immediately, and
  store it in a secret manager or environment variable.
regex: |
  \b(pat[[:alnum:]]{14}\.[a-f0-9]{64})\b
