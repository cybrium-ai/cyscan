id:        CBR-SEC-GL-GITLAB_PTT
title:     "Found a GitLab Pipeline Trigger Token, potentially compromising continuous integration workflows and"
severity:  high
languages: ['generic']
cwe:       ['CWE-798']
source:    gitleaks/gitlab-ptt
message: |
  Found a GitLab Pipeline Trigger Token, potentially compromising continuous integration workflows and project security.
  
  Remediation: Remove the secret, rotate it immediately, and
  store it in a secret manager or environment variable.
regex: |
  glptt-[0-9a-f]{40}
