id:        CBR-SEC-GL-SLACK_LEGACY_WORKSPACE_TOKEN
title:     "Identified a Slack Legacy Workspace token, potentially compromising access to workspace data and leg"
severity:  high
languages: ['generic']
cwe:       ['CWE-798']
source:    gitleaks/slack-legacy-workspace-token
message: |
  Identified a Slack Legacy Workspace token, potentially compromising access to workspace data and legacy features.
  
  Remediation: Remove the secret, rotate it immediately, and
  store it in a secret manager or environment variable.
pattern: |
  xox[ar]-(?:\d-)?[0-9a-zA-Z]{8,48}
