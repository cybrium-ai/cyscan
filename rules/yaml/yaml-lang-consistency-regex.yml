id:        CBR-YAML-LANG_CONSISTENCY_REGEX
title:     "Lang Consistency Regex"
severity:  high
languages: ['yaml']
source:    semgrep/lang-consistency-regex
message: |
  Found '$X' in language config which diverges from semgrep.dev normalization. Please use 'regex' instead.
pattern: |
  $X
fix: |
  regex
